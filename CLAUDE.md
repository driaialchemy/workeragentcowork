# CLAUDE.md — Agent Policy

> Draft generated by AIAlchemy Repo Governor for human review.
> Paste this file at the repository root. Do not commit until you have read it.

## Project Purpose

Planner-worker research briefing system focused on Claude Cowork / Claude Code cost optimization. Reads `sources.txt`, plans, summarizes, optionally emails, can run in Docker or GitHub Actions. Secrets in `.env` only.

**Repository:** `workeragentcowork`
**Risk Level:** HIGH

## Allowed Operations

- Read `README.md`, `ARCHITECTURE.md`, `orchestrator.py`, `planner.py`, `workers/`, and `utils/`.
- Edit prompts, sources list, or workers in small diffs after the HIGH walkthrough.

## Forbidden Operations

- Do **not** delete files without explicit user confirmation.
- Do **not** commit or push changes automatically.
- Do **not** read, print, or log the contents of `.env`, secrets, or credential files.
- Do **not** install packages outside a virtual environment without asking.
- Do **not** run destructive commands (`rm -rf`, `git reset --hard`, `DROP TABLE`, etc.).
- Do **not** send email from an unattended agent run.
- Do **not** read `.env` or print SMTP / OpenAI values.
- Do not push a workflow change that publishes secrets to GitHub Actions logs.

## Blocked Paths (never read contents)

- `.env`, `.env.*`, `.env.local`, `.env.production`
- `secrets.*`, `credentials.*`, `*.pem`, `*.key`
- Any file under `secrets/`, `private/`, or `credentials/`

## Environment

- **Language(s):** Python
- **Build / install:** requirements.txt; Docker optional

## In-scope edit areas

Prefer small edits in existing project folders. Stay out of `.git/`, virtualenvs, and secret files.

## Notes

- HIGH classification is from multi-provider and `api_key` *patterns* plus Postgres/SQLite mentions, not a committed `.env`.
- `.env.example` documents OPENAI_API_KEY and optional SMTP — keep real values local.

## How to verify

```bash
python -m py_compile orchestrator.py planner.py emailer.py
```

Always run the verification command before claiming a task is complete.
